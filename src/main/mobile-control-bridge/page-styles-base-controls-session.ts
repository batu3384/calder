export const MOBILE_PAGE_STYLES_BASE_CONTROLS_SESSION = `    .session-switch-row {
      margin-top: 12px;
      display: grid;
      grid-template-columns: minmax(0, 1fr) auto;
      gap: 8px;
      align-items: center;
    }
    .session-select {
      width: 100%;
      min-width: 0;
      border-radius: 10px;
      border: 1px solid var(--border);
      background: #060d1c;
      color: var(--text);
      padding: 10px 12px;
      font-size: 13px;
    }
    .session-switch-note {
      margin-top: 7px;
      font-size: 11px;
      color: var(--muted);
      min-height: 16px;
    }
`;
