# Superpowers docs (historical)

Plans, specs, and progress reports from the April 2026 architecture/debt sprint.

**Status:** Frozen archive. Many paths reference modules removed on **2026-07-27**, including:

- P2P share (`src/renderer/sharing/`, `share-dialog/`)
- Mobile remote (`apps/calder-mobile/`, `mobile-control-bridge/`, `mobile-surface/`)
- External CLI config writers (`config-watcher`, statusline templates, MCP governance write IPC)

**Current product/security posture:** see [`docs/reports/security/README.md`](../reports/security/README.md) and [`isolation-status.md`](../reports/security/isolation-status.md).

Files that mention removed modules include a `Historical (frozen)` banner at the top.
